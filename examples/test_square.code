# Square a number: N * N
# Cell 0 = input/counter (decremented each iteration)
# Cell 1 = N (original value, stays constant)
# Cell 2 = result (accumulator)

# Input
=72 . =105 . =58 . =32 .   # "Hi: "
>0 ,n                       # Read number into cell 0

# Copy to cell 1 (preserve original N)
cp                          # Copy cell 0 to register
>1                          # Move to cell 1 (now at position 1)
pt                          # Paste register to cell 1

# Initialize result to 0 (cell 2)
>1                          # Move right by 1 (now at position 2)
=0                          # Cell 2 = 0

# Square loop: add N to result, N times
>0                          # Go back to cell 0 (counter)
[                           # While counter != 0
  >1 cp                     # Go to cell 1, copy N to register
  >1 +a                     # Go to cell 2, add register to result
  >0 --                     # Go to cell 0, decrement counter
]

# Print result
=10 . =82 . =58 . =32 .     # newline, "R: "
>0 >2 :n                    # Go to cell 0, then cell 2, print result
=10 .                       # newline
